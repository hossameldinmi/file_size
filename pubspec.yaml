name: sized_file
version: 1.3.3

description: Lightweight and intuitive package for file size conversions and human-readable formatting. Convert between bytes, kilobytes, megabytes, gigabytes, and terabytes. Easy to use utils.

repository: https://github.com/hossameldinmi/sized_file

topics:
  - bytes
  - files
  - binary
  - storage
  - format

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies: null

dev_dependencies:
  test: ^1.26.3
